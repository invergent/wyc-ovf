import { AdminClaimsComponent } from './admin-claims.component';

describe('AdminClaimsComponent', () => {
  let component: AdminClaimsComponent;

  beforeEach(() => {
    component = new AdminClaimsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
