import { AdminSettingsComponent } from './admin-settings.component';

describe('AdminSettingsComponent', () => {
  let component: AdminSettingsComponent;

  beforeEach(() => {
    component = new AdminSettingsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
