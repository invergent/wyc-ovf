import { AdminProfileComponent } from './admin-profile.component';

describe('AdminProfileComponent', () => {
  let component: AdminProfileComponent;

  beforeEach(() => {
    component = new AdminProfileComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
