import { FormatPaneItemNamePipe } from '../format-pane-item-name.pipe';

describe('FormatPaneItemNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPaneItemNamePipe();
    expect(pipe).toBeTruthy();
  });
});
