import { ApprovalProgressBar } from './approval-progress-bar.component';

describe('Approval progress bar', () => {
  let component: ApprovalProgressBar;

  beforeEach(() => {
    component = new ApprovalProgressBar();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
